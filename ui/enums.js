export const SUBMIT_BUTTON_VISIBILITY = Object.freeze({
    AUTO: 1,
    NEVER: 2,
    ALWAYS: 3
});

export const GAME_TYPE = Object.freeze({
    MAIN: 1,
    PRACTICE: 2,
    ARCADE: 3,
    STORY: 4
});

export const GAME_FINISHED_STATE = Object.freeze({
    CLEAR: 1,
    FAILURE: 2
});

export const LIVES_ANIMATION_TYPE = Object.freeze({
    NONE: 1,
    SHAKE: 2,
    JUMP: 3
});